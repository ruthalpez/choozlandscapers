// import { Contractor, Score } from "@/types/contractor";

// // Helper function to get the highest visible Chooz score for a contractor
// export const getHighestChoozScore = (contractor: Contractor): number => {
//   const allScores: Score[] = contractor.scores || [];
//   if (allScores.length === 0) return 0;
//   return Math.max(...allScores.map((score: Score) => score.score || 0));
// };
